// {{ base_name }}_inference.cpp
// Example inference code - edit this file to match your specific inference needs
// This is a 'hello world' example that demonstrates how to use the generated model

#include "{{ base_name }}.h"
#include <iostream>
#include <iomanip>
#include <cstddef>

using namespace embedded_ml;
using namespace std;

int main() {
    // Example input data matching the model's input shape
    // Shape: [{{ input_shape }}]
    // TODO: Replace this with your actual input data
    float input[{{ base_name }}Model::kInputSize] = {
{% for idx in input_indices %}        0.0f{% if not idx.is_last %},{% endif %}{% if idx.needs_newline %}
{% endif %}{% endfor %}
    };

    // Output array to store inference results
    // Shape: [{{ output_shape }}]
    float output[{{ base_name }}Model::kOutputSize];

    // Run inference
    {{ base_name }}Model::Inference(input, output);

    // Print results
    cout << "Inference Results:" << endl;
    cout << fixed << setprecision(6);
    for (size_t i = 0; i < {{ base_name }}Model::kOutputSize; ++i) {
        cout << "  Output[" << i << "] = " << output[i] << endl;
    }

    // TODO: Process the output results as needed for your application
    // For example, find the class with highest probability:
    // size_t predicted_class = 0;
    // float max_prob = output[0];
    // for (size_t i = 1; i < {{ base_name }}Model::kOutputSize; ++i) {
    //     if (output[i] > max_prob) {
    //         max_prob = output[i];
    //         predicted_class = i;
    //     }
    // }
    // cout << "Predicted class: " << predicted_class << " (probability: " << max_prob << ")" << endl;

    return 0;
}

